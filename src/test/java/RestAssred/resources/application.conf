url = "https://reqres.in/"

age = 20

userParams{
    admin{
        login = "adminlogin"
        password = "adminpassword"
    }
    demo{
        login = "demologin"
        password = "demopassword"
    }
}